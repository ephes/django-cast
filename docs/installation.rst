============
Installation
============

At the command line::

    $ easy_install django-cast

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cast
    $ pip install django-cast
