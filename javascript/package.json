{
  "dependencies": {
    "vite": "^7"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:comments": "vite build -c vite.comments.config.ts",
    "build:all": "npm run build && npm run build:comments",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6",
    "@vue/test-utils": "^2",
    "jsdom": "^27",
    "vitest": "^4"
  }
}
